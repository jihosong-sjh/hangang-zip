{
  "name": "hangang-zip",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:api": "VITE_PARK_DATA_SOURCE=api VITE_API_BASE_URL=http://localhost:8081 vite",
    "dev:mock": "VITE_PARK_DATA_SOURCE=mock VITE_API_BASE_URL=http://localhost:8081 vite",
    "build": "node ./scripts/validate-build-env.mjs && tsc -b && vite build",
    "build:api": "node ./scripts/validate-build-env.mjs && tsc -b && vite build",
    "build:local": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.30.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.3",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
